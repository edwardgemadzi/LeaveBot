{
  "name": "leavebot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "client",
    "bot"
  ],
  "scripts": {
    "dev": "npm --workspace=client run dev",
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p public && rm -rf public/* && cp -r client/dist/* public/ && cd server && npm install && npm run build",
    "test": "npm --workspaces run test",
    "lint": "npm --workspaces run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "tsx": "^4.20.6"
  }
}
