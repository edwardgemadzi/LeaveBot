# API Configuration (optional - leave empty for same-origin API calls)
VITE_API_URL=

# Example for production:
# VITE_API_URL=https://your-app.vercel.app

# MongoDB Connection String (REQUIRED for production)
# Get this from MongoDB Atlas: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/leavebot?retryWrites=true&w=majority

# JWT Secret (CRITICAL - Change this in production!)
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=change-this-secret-immediately

# Optional: Create initial admin user from environment (Vercel Dashboard)
# If not set, first registered user becomes admin automatically
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_NAME=

# Security Notes:
# - Never commit the actual .env file to git
# - Use strong, unique JWT_SECRET in production
# - Store secrets in Vercel Environment Variables (not in code)
# - Rotate JWT_SECRET periodically
# - Admin credentials should ONLY be in environment, never in code
# - MongoDB connection string contains password - keep it secret!
